import { View } from "react-native";
import AppText from "../components/common/typography/app-text";

const NewNotesScreen = () => {
  return (
    <View>
      <AppText>Add a New Note</AppText>
    </View>
  );
};

export default NewNotesScreen;
