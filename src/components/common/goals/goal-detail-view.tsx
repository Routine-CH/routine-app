import { View } from "react-native";
import AppText from "../typography/app-text";

const GoalDetailView: React.FC = () => {
  return (
    <View style={{ backgroundColor: "red", width: "100%" }}>
      <AppText>GoalDetailView</AppText>
    </View>
  );
};

export default GoalDetailView;
