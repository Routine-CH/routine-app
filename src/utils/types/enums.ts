export enum StatusBarColor {
  light = "light",
  dark = "dark",
  auto = "auto",
  inverted = "inverted",
}

export enum ToastStyle {
  info = "infoToast",
  deletion = "successDeleteToast",
  success = "successToast",
  error = "errorToast",
}
